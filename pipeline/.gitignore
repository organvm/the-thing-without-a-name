# Derived and source-of-record artifacts never enter git.
#
# .work/ holds ~435 MB of 2017 originals plus private derivations. The originals are
# the artwork's provenance; they hydrate here from the exact private asset lock while
# remaining in independent archival custody. Public git is still the wrong custodian.
.work/

# Compiled Vision extractor (rebuild with 1_vision/build.sh).
1_vision/danse-vision
1_vision/*.dSYM/
